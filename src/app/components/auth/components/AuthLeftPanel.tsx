"use client";

import styles from "../styles/CustomAuth.module.css";

const AuthLeftPanel = () => {
  return (
    <div className={styles.leftPanel}>
      {/* Empty panel - can be customized with branding, images, or content later */}
    </div>
  );
};

export default AuthLeftPanel;

